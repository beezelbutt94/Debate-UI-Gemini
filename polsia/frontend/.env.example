# Inlined into the browser bundle at build time.
NEXT_PUBLIC_API_URL=http://localhost:8000
# Only for local/private deployments: anything NEXT_PUBLIC_ is visible to every visitor.
NEXT_PUBLIC_OPERATOR_TOKEN=
