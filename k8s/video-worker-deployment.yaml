# GPU-accelerated Celery worker deployment. Requires a node pool with
# NVIDIA GPUs and the NVIDIA device plugin installed; see
# k8s/overlays/production/patches/worker-gpu.yaml for node affinity.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: viral-trending-video-worker
  namespace: production
  labels:
    app: viral-trending-video-worker
spec:
  replicas: 2
  selector:
    matchLabels:
      app: viral-trending-video-worker
  template:
    metadata:
      labels:
        app: viral-trending-video-worker
    spec:
      containers:
        - name: celery-worker
          image: ghcr.io/viral-trending/worker:latest
          imagePullPolicy: IfNotPresent
          command:
            - "celery"
            - "-A"
            - "app.workers.celery_app.celery_app"
            - "worker"
            - "--loglevel=INFO"
            - "-Q"
            - "premium_sla,standard_jobs,draft_preview"
            - "-c"
            - "2"
            - "-E"
          resources:
            limits:
              nvidia.com/gpu: 1
              memory: 16Gi
              cpu: "8"
            requests:
              nvidia.com/gpu: 1
              memory: 8Gi
              cpu: "4"
          envFrom:
            - secretRef:
                name: viral-trending-production-secrets
            - configMapRef:
                name: viral-trending-production-config
          volumeMounts:
            - name: scratch-space
              mountPath: /tmp/viral_trending_production
      volumes:
        - name: scratch-space
          emptyDir:
            medium: Memory
            sizeLimit: 10Gi
