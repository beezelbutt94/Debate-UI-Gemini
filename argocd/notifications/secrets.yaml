# Template only -- replace both placeholders before applying.
apiVersion: v1
kind: Secret
metadata:
  name: argocd-notifications-secret
  namespace: argocd
type: Opaque
stringData:
  slack-token: "REPLACE_WITH_SLACK_BOT_OAUTH_TOKEN"
  discord-webhook-url: "REPLACE_WITH_DISCORD_INCOMING_WEBHOOK_URL"
