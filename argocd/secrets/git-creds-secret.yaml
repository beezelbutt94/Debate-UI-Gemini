# Template only -- credentials for argocd-image-updater's write-back to
# this repo. Replace the placeholder before applying; never commit a
# filled-in version.
apiVersion: v1
kind: Secret
metadata:
  name: git-updater-credentials
  namespace: argocd
type: Opaque
stringData:
  username: "viralvision-automation[bot]"
  password: "REPLACE_WITH_GITHUB_PAT_REPO_CONTENTS_WRITE"
